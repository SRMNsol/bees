# The default OpsWorks install provides an older version of Apache & PHP and when we install
# a newer version it conflicts with this module.  When OpsWorks gets updated then we can remove
# this recipe and let it configure everything properly again.